org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true

# ==================== ПАРАМЕТРЫ СБОРКИ ====================
# Меняешь эти строки — меняются и имя приложения, и версия: значения расходятся отсюда
# и в манифест Android, и в C++ (через -D в CMake), поэтому «как называется игра»
# определено ровно в одном месте.
appName=OSIL Survival
appVersion=0.1.0
