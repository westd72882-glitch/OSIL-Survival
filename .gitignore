# Сборка
build/
cmake-build-*/
*.o
*.a
*.so

# Данные сервера
world.db
world.db-journal
logs/
*.log

# Предпросмотр карты (osil_mapgen) — только то, что утилита кладёт в корень.
# Правило намеренно привязано к корню: без косой черты оно съедало и картинки в
# docs/images/, и они молча не попадали в репозиторий.
/map_*.png
/map_*.txt

# Редакторы
.vscode/
.idea/
*.swp

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/java/org/libsdl/
*.apk

# Кэш питоновских утилит
__pycache__/

# Аккаунты сервера: пароли и соли, в репозитории им не место
accounts.txt
