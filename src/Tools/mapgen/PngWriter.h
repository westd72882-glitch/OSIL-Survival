#pragma once
// ==================== ЗАПИСЬ PNG БЕЗ ЗАВИСИМОСТЕЙ ====================
// Утилите предпросмотра нужно отдать картинку карты, которую откроет любой просмотрщик
// и покажет GitHub. Тянуть в проект libpng/zlib ради одной картинки не хочется: сервер
// собирается на голой системе одной командой, и каждая новая зависимость — это ещё один
// способ сломать сборку у администратора.
//
// PNG допускает поток deflate, состоящий из «сохранённых» (несжатых) блоков — это
// законный zlib-поток. Пишем именно так: нужен только CRC-32 (для чанков PNG) и
// Adler-32 (контрольная сумма zlib), оба в несколько строк. Файл выходит крупнее
// сжатого, но это карта для глаз, а не ассет игры.
#include <cstdint>
#include <string>
#include <vector>

// pixels — RGB по 3 байта на пиксель, строк height, в строке width пикселей.
bool writePng(const std::string& path, int width, int height, const std::vector<uint8_t>& rgb);
